# Render Free Test Copy

This is the free/demo-oriented Render copy. Start with `README_RENDER_FREE_TEST.md`.

This setup is not safe for real production data because file storage and backups are ephemeral on Render free web services.
